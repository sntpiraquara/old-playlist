CREATE DATABASE playlist;

USE playlist;

CREATE table playlist(
id integer ,
nome varchar(255),
artista varchar(255),
tipo varchar(255),
primary key(id)
);


# Neri a estrutura do database foi alterada corretamente?
